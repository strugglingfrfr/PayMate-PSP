# CRE Workflow Settings — PayMate Pool Orchestrator

staging-settings:
  user-workflow:
    workflow-name: "paymate-pool-orchestrator-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"
